# secrets -- never commit
credentials.yml
.env

# the clipboard relay writes the last paste here in clear
last_prompt.txt
**/last_prompt.txt
tufin_raw.json

# local state
*.db
data/*.db
.files/
**/.files/
__pycache__/
*.py[cod]
.venv/
venv/

# chainlit regenerates these on first run
.chainlit/translations/
**/.chainlit/translations/
.chainlit/.langchain.db

# editors
.vscode/
.idea/

# frontend
frontend/node_modules/
vscodelm-bridge/node_modules/
vscodelm-bridge/*.vsix
frontend/dist/

# config backups: same reason as .env itself
.env.*
!.env.example

# What has worked on this estate's platforms, written by the agent
# itself. Only shapes are stored -- addresses, interfaces and names are
# masked out -- but it is learned from YOUR devices, so it stays on the
# machine that learned it unless you decide otherwise.
data/command_notebook.json
