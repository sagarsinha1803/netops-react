# Pinned to what the working machine runs. Install with:
#   uv pip install -r requirements.txt

fastapi==0.141.1
uvicorn[standard]==0.52.1
langgraph==1.2.9
langchain-core==1.5.0
langchain-mcp-adapters==0.3.0
langchain-openai==1.4.0
mcp==1.28.1
fastmcp==3.4.4
pydantic==2.13.4
requests==2.34.2
PyYAML==6.0.3
python-dotenv==1.2.2
pyperclip==1.11.0
urllib3==2.7.0

# paramiko is NOT here: only the host that actually runs
# troubleshoot_agent_mcp.py needs it. See requirements-device.txt.

# langchain-openai 1.4.0 needs openai<3. Anything that drags in a newer one
# breaks every API call with an AssertionError inside httpx, surfaced only as
# "Connection error" -- so pin it rather than discover that again.
openai>=2.45,<3
